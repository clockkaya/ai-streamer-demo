# ============================================================================
#  .env.example — 环境变量模板（提交到 Git，供团队成员参考）
#
#  使用方法: cp .env.example .env  然后填入真实值
#
#  加载链: 环境变量 > .env.{ENVIRONMENT} > .env > config.py 默认值
#  切换环境: 修改 ENVIRONMENT 值为 dev / test / prod
# ============================================================================

# ── 必填：敏感凭证 ──────────────────────────────────────────────────
GEMINI_API_KEY=<your-gemini-api-key>

# ── 运行环境（dev / test / prod）──────────────────────────────────────
ENVIRONMENT=dev

# ── 以下为可选覆盖（通常放在 .env.dev / .env.test / .env.prod 中）──
# PORT=8000
# GEMINI_MODEL=gemini-2.5-flash-lite
# EMBEDDING_MODEL=gemini-embedding-001
# TTS_VOICE=zh-CN-XiaoyiNeural
# KNOWLEDGE_FILE=data/knowledge.txt
# LOG_LEVEL=DEBUG
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB_NAME=ai_streamer