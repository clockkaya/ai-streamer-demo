# ============================================================================
#  .env.example — 环境变量模板（提交到 Git，供团队成员参考）
#
#  使用方法: cp .env.example .env  然后填入真实值
#
#  引用关系:
#    .env  ──读取──>  app/core/config.py (pydantic-settings 自动加载)
#    .env  ──读取──>  docker-compose.yml (Compose 变量替换 + env_file)
# ============================================================================

# ── 必填：敏感凭证 ──────────────────────────────────────────────────
GEMINI_API_KEY=<your-gemini-api-key>

# ── 运行环境（development / testing / production）────────────────────
ENVIRONMENT=development

# ── 网络代理（可选，不需要则留空或删除）──────────────────────────────
# HTTP_PROXY=http://127.0.0.1:33210
# HTTPS_PROXY=http://127.0.0.1:33210

# ── 以下为可选覆盖，不设置则使用 config.py 中的默认值 ──────────────
# PORT=8000
# GEMINI_MODEL=gemini-2.5-flash
# EMBEDDING_MODEL=gemini-embedding-001
# TTS_VOICE=zh-CN-XiaoyiNeural
# KNOWLEDGE_FILE=data/knowledge.txt
# LOG_LEVEL=INFO